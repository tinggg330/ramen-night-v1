# 泡面夜宵 V1 视觉系统

## 方向

- 竖屏 9:16、4399 页游式卡通插画，不追求写实质感。
- 深蓝夜景负责安静氛围，奶油黄和橘红只用于食物、确认状态和主按钮。
- 保留确认稿的“卧室、电视、猫、桌面泡面”叙事，不把页面做成通用组件库界面。

## 字体与文案

- 标题和主要按钮：`Kaiti SC / STKaiti / KaiTi`，模拟手写感。
- 辅助信息：系统中文无衬线字体，保证小字号清晰。
- 首页文案、标题和入口名称与确认稿一致；选择流程使用短句和口语提示。

## 色彩

- 夜色：`#0d1124`、`#172448`、`#27345b`
- 奶油白：`#fff6df`
- 主按钮黄：`#ffd96b`，底部阴影 `#783f2d`
- 强调橘：`#ff8f5e`
- 便签粉：`#ffc1bb`

## 组件规则

- 主按钮使用厚描边和实体底部阴影，按下时下移 4px。
- 选择卡用深蓝卡片；选中时只增加黄色描边和勾选，不大幅改变布局。
- 图标按钮最小 43×43px，适合单手点击。
- 所有游戏阶段都复用顶部返回、阶段名和声音开关。
- 动画只用于蒸汽、杯盖和点击反馈；用户开启“减少动态效果”后自动停用。

## 素材策略

- 正式插画素材均从 `public/assets/` 读取。
- 汤底和面种共享杯体；三种面使用各自的透明 PNG，在选择、制作、揭盖与吃面阶段保持一致。按 PNG 留白分别校准显示尺寸。
- 熟面采用三种面 × 100 / 70 / 40 / 10 四档独立透明插画（`cooked-{fried,udon,spinach}-{100,70,40,10}.png`），共 12 张，画布统一为 1536 × 1024。各档固定显示尺寸与锚点，保留面条粗细，通过不规则缺口、前侧残留与零散面条表现吃掉的过程，不整体缩小。
- 吃面第 0–1 / 2–3 / 4–5 / 6 口分别显示四档，第 7 口清空面层。配菜按固定位置逐组减少，剩余配菜不重新排布；汤层持续保留，最后进入喝汤与空杯。
- 放面阶段不显示汤；倒水后显示所选汤色，面周围保留窄圈汤。汤底基于原汤素材调色，豚骨叠奶白底色。
- 调料包使用可缩放 SVG 与 CSS 单次动画：倾倒、颗粒落下并停留，不融化、不扩散。约 2.3 秒后开放下一步，减少动态效果时直接显示落地状态。
- 芝士单选采用两片大面积错叠，多选时铺在其他配菜下方。
- ASMR 音效目录已预留；当前仅接入循环卧室背景音。

### 喝汤液面

喝汤显示满汤、第一口后的中液面、最后一口的低液面、空杯四个状态。红烧、豚骨、麻辣各使用独立的 `cup-mid-soup-*` 和 `cup-last-soup-*` 完整杯体插画，液面在杯内下降并被前侧杯壁遮挡，不缩放汤层。封面继续使用独立的满碗食物图；杯盖锚点不随液面改变。
