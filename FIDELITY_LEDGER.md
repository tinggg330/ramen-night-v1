# 主画面还原校对记录

对照基准：`../ramen-night-assets/reference/main-screen-approved.png`

## 五个最影响观感的差异与处理

1. **泡面主体一开始只有面层、像悬浮的盘子**  
   已增加独立红色杯身作为底层，再把面层按杯口比例覆盖，恢复确认稿中的杯面体积感。

2. **完整原图的泡面杯占比很大，但会挤压网页可点击区域**  
   V1 将杯体缩小约一档，保留“泡面是主角”的视觉重心，同时让 320px 高度下主按钮与三个底部入口完整可见。这是为真实交互做的有意偏差。

3. **确认稿带手机、饮料罐和游戏手柄，干净背景没有这些前景物**  
   首页不重新堆叠所有小物，以免遮挡 V1 动态泡面；保留卧室、电视、猫、月亮和植物形成的核心夜间叙事。独立小物仍在素材包，可在后续装饰层继续加入。

4. **原稿标题和按钮是手绘字形，网页系统字会显得过于现代**  
   首页标题直接使用确认后的贴纸 PNG；正文、按钮和导航采用楷体候选栈，其他小字号才使用清晰的系统无衬线字体。

5. **原稿只有一个静态首页，V1 需要十个交互阶段**  
   新增页面沿用同一深蓝、奶油黄、橘红、厚描边和实体阴影语言；每屏继续露出房间背景，避免进入选择流程后变成通用表单界面。

## 已验证尺寸

- 390 × 844：首页完整显示，文字、泡面、便签、主按钮和三项底部入口均在首屏。
- 320 × 700：核心内容无横向溢出，主按钮和底部入口可点击；选择页三张汤底卡和确认按钮完整显示。

## 已验证流程

首页 → 选汤底 → 选面 → 选 0–3 种配菜 → 6 步制作 → 20 秒等待/点击加速 → 揭盖 → 7 口吃面 → 3 口喝汤 → 完成页 → 再来一碗。

背景音在首次进入游戏后开始，循环播放；设置弹窗可关闭并持久记住声音选择。当前没有嗦面、喝汤与碰杯音效，目录和交互触发点留待素材补齐后接入。

## 2026-09-15 对齐修正

- 杯身、杯口面层、配菜和杯盖现在共用同一个方形舞台与中心锚点。
- 面层压缩到杯口椭圆内部，不再超出杯沿或与杯身错位。
- 打开杯面、盖好杯盖、等待和揭盖页面均明确保留红色杯身。
- `+0 配菜` 路径改用无配菜面层，不再出现鸡蛋、午餐肉、青菜或鱼板。
- 独立配菜的锚点上移到杯口范围，避免落在杯身文字上。
- 完成页点击“吃饱了，去睡觉”只返回首页，不再自动开启并持久化暗色的晚安模式。

## 2026-09-15 统一坐标系复检

- 撤掉运行时的 `ramen-lid-half-open.png` 组合素材；它自身同时含有掀起的盖片和下方椭圆，叠在杯身上必然出现双杯口与漂移。
- 首页、六步制作、等待、揭盖、吃面和喝汤统一改用同一个 `RamenCupVisual`：固定杯身、杯口椭圆、面层、配菜、杯盖、蒸汽和筷子的相对锚点。
- 闭合杯盖只覆盖统一杯口，不再拥有各页面独立的尺寸与偏移。
- 蒸汽统一从杯口中心升起；筷子尖端落在面层内部；揭盖页的首个配菜移到右侧可见区域，避免被打开的杯盖遮住。
- 实际浏览器逐页截图复检：首页、打开杯面、倒入热水、盖好杯盖、等待、揭盖和开吃页面均已核对，不再只用流程通过作为视觉验收。
- 后续截图复检发现 CSS 人工绘制的 `.cup-rim` 与杯身素材自带杯沿形成错位双圈；已彻底删除该 DOM/CSS 层，只保留杯身原生杯沿。
- 开盖素材改用自身宽高比并重新落在左侧杯沿连接点；闭合盖加高并轻微横向扩展，使外轮廓覆盖杯口且不再悬空露出不均匀白边。

## 2026-09-16 首页与开盖素材重做

- 首页泡面靠左的根因是 `.cup-visual { inset: 0 }` 的通用规则在后方覆盖了首页定位；改用更明确的 `.cup-visual.home-cup-visual`，以 `left: 50%` 和 `translateX(-50%)` 居中。
- 浏览器实测 430px 视口中，手机内容中心为 `390.5px`，杯身中心为 `392.63px`，误差约 `2.13px`。
- 原开盖素材的连接结构与杯口透视不匹配，停止继续用位移硬贴；重新绘制 `ramen-lid-open-perspective.png`，把连接点放在素材下方中部，与杯口左前沿接触。
- 新素材已确认包含真实 Alpha 通道；首页首次打开和制作流程第 1/6 步均重新截图审核。

## 2026-09-16 杯盖合成后再扣图

- 改用“先完成整碗合成，再从合成图原位扣出杯盖”的制作顺序，避免独立绘制后靠 CSS 猜测透视。
- 合成参考保存为 `public/assets/reference/ramen-open-lid-composite-reference.png`：杯盖保持原版半揭开的程度，连接边落在杯口左侧杯沿。
- 最终透明素材为 `public/assets/ramen/ramen-lid-open-from-composite.png`，确认具备真实 Alpha 通道。
- 首页与制作流程第 1/6 步均重新截图检查：杯盖覆盖杯口左侧、连接点落在杯沿，没有接到面条上。

## 2026-09-16 杯盖坐标复核更正

- 上一轮虽使用了正确的合成参考和扣图素材，但网页中的 `left: -10%`、`top: -10%`、`width: 74%` 是目测值，不能作为已对齐结论；用户并排截图证明连接位置仍有偏差。
- 本轮不重画素材，改为从合成参考中量取杯盖相对杯身边界的比例：`left: -9.9%`、`top: -19%`、`width: 72.4%`、`height: 52%`。
- 首页杯面容器统一为参考合成图的 `1274 / 1235` 比例，避免首页的非等比容器让同一杯盖坐标发生漂移。
- 在实际浏览器重新检查首页、制作流程 1/6 和揭盖页：杯盖尖角位于杯口左侧外沿，杯盖不接触面条，杯身保持居中。

## 2026-09-17 分阶段吃面素材补齐

- 新增方便面、乌冬、菠菜面各四档熟面插画，共 12 张透明 PNG；以不规则空缺和零散残留表现食量减少，移除随剩余食量缩小整层的处理。
- 三种面所有四档均在真实页面检查加载与截图：390 × 844 检查红烧方便面及麻辣菠菜面，320 × 700 检查豚骨乌冬；同一视口各档面层位置与尺寸一致。
- 配菜保留原位置并逐组减少；检查了无配菜、蛋/午餐肉/青菜、芝士/葱花三种组合。方便面完整走通七口吃面、三口喝汤及完成页。
- 浏览器检查未发现警告或错误。以上为实现与页面检查记录，未代表用户对最终画面的视觉验收。
